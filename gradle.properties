# 启用 AndroidX 支持（必须）
android.useAndroidX=true
# 启用 Jetifier，自动将第三方库转换为 AndroidX（必须）
android.enableJetifier=true

# --- 可选优化（防止某些 Gradle 8.x 的缓存警告）---
# 如果还有关于缓存的警告，可以加上这行
org.gradle.configuration-cache=warn
